// data/examQuestionBank.mjs
// Canonical source for the ctracer Practice Exam question bank.
// SERVER-ONLY content: loaded into Firestore (collection: exam_questions) and
// delivered via Cloud Functions. NEVER imported by web/ (answer-key isolation).
// Design: docs/superpowers/specs/2026-05-30-exam-question-bank-design.md
//
// Variance budget per option (first pass):
//   correct option   -> practice: 2 wordings, timed: 1 wording
//   distractor option -> practice: 1 wording, timed: 2 wordings
// (high timed variance comes from varying the three distractors per attempt)

export const BLUEPRINT = { d1: 16, d2: 12, d3: 12, d4: 11, d5: 9 }

export const TOPICS = {
  d1: ['Multi-agent patterns', 'Agentic loops', 'Session management', 'Tool orchestration', 'Parallelization workflows', 'Chaining workflows', 'Routing workflows', 'Fallback loop design', 'Cost optimization'],
  d2: ['CLAUDE.md hierarchies', 'Custom slash commands', 'Agent Skills', 'Hooks', 'MCP server integration', 'GitHub integration', 'Context management', 'Claude Code SDK', 'Subagents', 'Tool access restrictions', 'Configuration files'],
  d3: ['System prompts', 'XML tags', 'Few-shot examples', 'Chain-of-thought', 'JSON schema enforcement', 'Structured output', 'Validation retry loops', 'Hallucination prevention', 'Failure modes', 'Prompt evaluation', 'Temperature & sampling', 'Prompt caching'],
  d4: ['Tool use fundamentals', 'Multi-tool systems', 'MCP architecture', 'MCP server building', 'MCP primitives', 'MCP client implementation', 'Static vs templated resources', 'MCP Server Inspector', 'Web search tool', 'Tool-use error handling', 'Tool selection'],
  d5: ['Context window limits', 'Model selection', 'Prompt caching', 'RAG fundamentals', 'Context discipline', 'Streaming responses', 'Rate limits & errors', 'Extended thinking', 'Image and PDF support', 'Citations', 'Files API & Code Execution', 'Output evaluation'],
}

// Populated domain-by-domain. Shape: see spec §3.
export const QUESTIONS = [
  // ─── Domain 1 — Agentic Architecture & Orchestration (16) ──────────────────
  {
    id: 'd1-001', domain: 'd1', topic: 'Multi-agent patterns',
    archetype: 'what-breaks', difficulty: 'scenario',
    dimensions: ['critical-eval', 'judgment'], claudeSpecific: false,
    stem: [
      'An orchestrator spawns five subagents that all read and write the same shared scratchpad document concurrently. Results are sometimes corrupted or silently overwritten. What is the best fix?',
      'A team runs five parallel subagents that share one mutable working document; outputs occasionally clobber each other. Which change addresses the root cause?',
    ],
    correctIndex: 0,
    options: [
      { practice: ['Give each subagent an isolated workspace and have the orchestrator merge their returned results.', 'Isolate each subagent’s output, then let the orchestrator combine the results deterministically.'],
        timed: ['Hand every subagent its own scratch space and aggregate the separate outputs in the orchestrator.'] },
      { practice: ['Run the five subagents strictly one after another so writes never overlap.'],
        timed: ['Serialize all subagents end-to-end to avoid concurrent writes.', 'Force the subagents to run sequentially so no two write at once.'] },
      { practice: ['Add a retry that re-runs a subagent whenever the document looks corrupted.'],
        timed: ['Detect corruption and re-execute the affected subagent until it looks clean.', 'Add a corruption check that re-runs a subagent whenever the document looks wrong.'] },
      { practice: ['Switch every subagent to the most capable model so they make fewer mistakes.'],
        timed: ['Upgrade all subagents to the top model tier to reduce errors.', 'Move every subagent to the most capable model to cut mistakes.'] },
    ],
    explanation: 'Concurrent writes to shared mutable state cause races; the fix is isolation plus a single merge point, which preserves parallelism. Serializing throws away the whole reason to parallelize; retrying papers over the race without removing it; a bigger model does not fix a concurrency bug.',
  },
  {
    id: 'd1-002', domain: 'd1', topic: 'Multi-agent patterns',
    archetype: 'critique-output', difficulty: 'hard',
    dimensions: ['critical-eval', 'judgment'], claudeSpecific: false,
    stem: [
      'An AI assistant proposes this design to answer one user question: a single orchestrator calls 12 specialized subagents in sequence, each receiving the full running transcript. What is the most serious problem?',
      'Reviewing an AI-proposed architecture — 12 subagents chained sequentially, each fed the entire growing transcript, to handle a single query — what is the biggest issue?',
    ],
    correctIndex: 0,
    options: [
      { practice: ['Feeding the full transcript to 12 sequential agents explodes context, latency, and cost for a task that likely needs one or two; right-size the agent count and scope each agent’s context.', 'It is over-engineered: unbounded context growth across 12 serial calls for a job a single well-scoped agent could do.'],
        timed: ['Twelve serial agents each receiving the whole transcript balloon cost and latency for a task that needs far fewer; reduce the count and narrow each agent’s inputs.'] },
      { practice: ['It uses too few specialized agents; accuracy would improve with even more of them.'],
        timed: ['There are not enough agents — add more specialists for better accuracy.', 'Accuracy would rise with additional specialized agents in the chain.'] },
      { practice: ['The orchestrator should call the largest model for every subagent step.'],
        timed: ['Each subcall should be upgraded to the top model tier.', 'Promote every subagent call to the strongest model.'] },
      { practice: ['It is missing a dedicated agent whose only job is to format the final answer.'],
        timed: ['A separate formatting-only agent should be appended to the chain.', 'Add a dedicated final agent whose sole job is formatting the answer.'] },
    ],
    explanation: 'The headline failure is over-decomposition: serial fan-out with full-transcript context multiplies latency and tokens with no accuracy payoff. More agents, a bigger model, or a formatting agent all make it worse or miss the point. Recognizing needless complexity is the judgment signal.',
  },
  {
    id: 'd1-003', domain: 'd1', topic: 'Agentic loops',
    archetype: 'what-breaks', difficulty: 'scenario',
    dimensions: ['critical-eval', 'judgment'], claudeSpecific: false,
    stem: [
      'An agentic loop continues until the model declares the task complete. On ambiguous tasks it sometimes loops dozens of times, editing and re-editing the same files. What should be added?',
      'A loop that runs until the model says "done" occasionally thrashes for dozens of iterations on unclear tasks. Which addition fixes this?',
    ],
    correctIndex: 0,
    options: [
      { practice: ['A hard iteration ceiling plus explicit, checkable completion criteria the loop evaluates each pass.', 'A maximum-iteration cap and concrete done-conditions verified on every pass.'],
        timed: ['An upper bound on iterations together with objective completion checks each cycle.'] },
      { practice: ['A stronger prompt telling the model to be more confident about when it is finished.'],
        timed: ['Prompt the model more firmly to decide when it is done.', 'Strengthen the instruction so the model judges completion better.'] },
      { practice: ['A larger context window so the loop can keep more history before stopping.'],
        timed: ['More context capacity so the loop retains more state before terminating.', 'A bigger context window so more history is kept before the loop ends.'] },
      { practice: ['Streaming responses so the user can watch each iteration as it happens.'],
        timed: ['Enable token streaming so iterations are visible in real time.', 'Stream the output so each loop pass can be watched live.'] },
    ],
    explanation: 'Self-declared completion with no ceiling is the classic runaway-loop flaw; a bounded iteration count plus objective exit criteria fixes it. Prompting harder still trusts the same unreliable signal; more context or streaming changes nothing about termination.',
  },
  {
    id: 'd1-004', domain: 'd1', topic: 'Agentic loops',
    archetype: 'best-practice', difficulty: 'core',
    dimensions: ['judgment'], claudeSpecific: false,
    stem: [
      'When designing a reliable agentic loop, which control structure is recommended?',
      'What is the recommended shape for an agentic loop that needs to stay reliable?',
    ],
    correctIndex: 0,
    options: [
      { practice: ['Plan, act, observe, and check against the goal each iteration, with a bounded number of iterations.', 'A bounded plan-act-observe-verify cycle that re-checks the goal every pass.'],
        timed: ['Iterate through plan → act → observe → evaluate-goal, capped at a maximum number of cycles.'] },
      { practice: ['Generate the entire plan once up front and execute it without re-checking.'],
        timed: ['Produce one fixed plan and run it straight through with no re-evaluation.', 'Plan everything once and execute without checking progress.'] },
      { practice: ['Repeat the same action until the output stops changing between iterations.'],
        timed: ['Keep re-running the same step until its output is stable.', 'Repeat one action until consecutive outputs match.'] },
      { practice: ['Avoid loops entirely and force every task into a single one-shot response.'],
        timed: ['Never loop — always handle the task in one shot.', 'Forbid iteration and force a single-pass response every time.'] },
    ],
    explanation: 'A bounded observe-and-re-check cycle keeps the agent grounded against the goal and guarantees termination. A fixed up-front plan cannot adapt to observations; spinning until output stabilizes is not a goal check; banning loops abandons the agentic pattern where it is warranted.',
  },
  {
    id: 'd1-005', domain: 'd1', topic: 'Session management',
    archetype: 'what-breaks', difficulty: 'scenario',
    dimensions: ['critical-eval', 'judgment'], claudeSpecific: false,
    stem: [
      'A multi-turn agent stores its entire growing message history and replays all of it on every turn. After ~30 turns it slows down, costs spike, and it starts losing the thread. What is the best fix?',
      'An agent replays its full and ever-growing transcript each turn; by turn 30 it is slow, expensive, and increasingly incoherent. Which change helps most?',
    ],
    correctIndex: 0,
    options: [
      { practice: ['Summarize/compact older turns and keep a rolling working context, persisting key facts separately.', 'Compress earlier turns into a running summary and carry forward only the durable key state.'],
        timed: ['Roll up old turns into a compact summary and maintain a bounded working window plus persisted essentials.'] },
      { practice: ['Start a brand-new session on every turn so history never accumulates.'],
        timed: ['Reset to a fresh session each turn to prevent buildup.', 'Begin a new session every turn so nothing accumulates.'] },
      { practice: ['Move to the largest-context model and keep replaying the entire history.'],
        timed: ['Adopt the biggest context window and continue sending the full transcript.', 'Switch to a larger-context model while still replaying everything.'] },
      { practice: ['Lower the temperature so the model stays more focused as history grows.'],
        timed: ['Reduce sampling temperature to keep focus as the transcript expands.', 'Lower temperature so the model stays on track as history grows.'] },
    ],
    explanation: 'Unbounded replay degrades cost, latency, and focus; summarization plus durable key-state is the standard remedy. Resetting every turn destroys continuity; a bigger context only postpones the same failure; temperature has nothing to do with context bloat.',
  },
  {
    id: 'd1-006', domain: 'd1', topic: 'Tool orchestration',
    archetype: 'what-breaks', difficulty: 'scenario',
    dimensions: ['critical-eval'], claudeSpecific: false,
    stem: [
      'An agent calls tools in a fixed sequence. When tool 2 returns an error, the agent ignores it and feeds tool 2’s empty output into tool 3, producing garbage downstream. What should change?',
      'In a fixed tool chain, a failure from the second tool is ignored and its empty result flows into the third tool, corrupting the rest. Which fix is correct?',
    ],
    correctIndex: 0,
    options: [
      { practice: ['Detect the tool error and return it to the model so it can retry, choose another tool, or abort — do not pass failed output forward.', 'Surface tool failures back to the model to decide the next step instead of forwarding empty results.'],
        timed: ['Catch the failed tool result, hand the error to the model, and let it retry/branch/stop rather than chaining bad output.'] },
      { practice: ['Retry tool 2 immediately and indefinitely until it eventually succeeds.'],
        timed: ['Keep re-invoking tool 2 with no limit until it returns success.', 'Retry tool 2 endlessly until it finally succeeds.'] },
      { practice: ['Remove tool 2 from the chain so the failure can never occur.'],
        timed: ['Drop tool 2 entirely to eliminate the error path.', 'Remove tool 2 from the sequence so it cannot fail.'] },
      { practice: ['Run all three tools in parallel so a failure in one does not block the others.'],
        timed: ['Execute the tools concurrently so one failure cannot stall the rest.', 'Run all tools in parallel to keep one failure from blocking the others.'] },
    ],
    explanation: 'The flaw is swallowing a tool error and propagating bad data; the fix is explicit error handling that informs the model. Unbounded retry can hang; deleting the tool removes needed capability; parallelizing does not address dependency on tool 2’s result and still forwards garbage.',
  },
  {
    id: 'd1-007', domain: 'd1', topic: 'Fallback loop design',
    archetype: 'what-breaks', difficulty: 'scenario',
    dimensions: ['critical-eval', 'judgment'], claudeSpecific: false,
    stem: [
      'An agent retries a failing tool call in a tight loop, re-issuing the identical call immediately until it succeeds. Under intermittent API failures, some sessions run away on cost and never terminate. What should the design add?',
      "A team's agent handles a flaky API by immediately re-calling it on every failure with no other change. Some sessions hang and rack up cost. Which change best fixes this?",
    ],
    correctIndex: 0,
    options: [
      { practice: ['Add bounded retries with exponential backoff and an escape hatch that exits after N attempts.', 'Cap retries with backoff plus a fallback path once the attempt limit is hit.'],
        timed: ['Introduce a retry ceiling with exponential backoff and a defined exit/fallback when the ceiling is reached.'] },
      { practice: ['Switch every step to the highest-capability model so calls fail less often.'],
        timed: ['Route all steps to the top model tier to reduce failure frequency.', 'Send every step to the most capable model so failures drop.'] },
      { practice: ['Remove the loop and call the tool once, failing the whole task on any error.'],
        timed: ['Drop retries entirely and abort the task on the first failure.', 'Remove all retrying and fail the task on any single error.'] },
      { practice: ['Wrap the same immediate-retry loop in a parallel agent to amortize cost.'],
        timed: ['Parallelize the existing no-backoff loop across agents to spread the load.', 'Fan the same immediate-retry loop out across agents to share the cost.'] },
    ],
    explanation: 'Domain 1 fallback-loop design: bounded retries + backoff + an escape hatch. The distractors are anti-patterns an AI assistant tends to generate - top-tier-everywhere (cost, does not fix non-termination), brittle no-retry over-correction, and parallelizing a broken loop. Catching the missing escape hatch is the critical-evaluation signal.',
  },
  {
    id: 'd1-008', domain: 'd1', topic: 'Fallback loop design',
    archetype: 'critique-output', difficulty: 'hard',
    dimensions: ['critical-eval', 'judgment'], claudeSpecific: false,
    stem: [
      'An AI assistant writes this fallback: on any error, fall back to a cheaper model, then to a still-cheaper one, with no attempt limit and no logging. Reviewing it, what is the biggest concern?',
      'Code review: a fallback chain silently downgrades to progressively cheaper models on every error, unbounded and unlogged. What is the most important problem?',
    ],
    correctIndex: 0,
    options: [
      { practice: ['It degrades quality silently with no limit or visibility — add a quality floor, log/surface each fallback, and a terminal give-up path.', 'Silent unbounded downgrade hides failures; it needs an observability signal, a minimum-acceptable tier, and a final failure state.'],
        timed: ['Unlogged, unlimited degradation can return poor answers with no signal — introduce logging, a tier floor, and a defined stop.'] },
      { practice: ['It should fall back to progressively more expensive models instead of cheaper ones.'],
        timed: ['The escalation direction is wrong — it should climb to costlier models on failure.', 'It ought to fall back upward to more expensive models, not cheaper ones.'] },
      { practice: ['Fallbacks are unnecessary; the call should simply fail on the first error.'],
        timed: ['There is no need for fallback at all — fail immediately on any error.', 'Skip fallback entirely and error out on the first failure.'] },
      { practice: ['The only issue is that it does not retry the same model once before falling back.'],
        timed: ['Its sole flaw is skipping a single same-model retry before degrading.', 'The only gap is one missing same-model retry prior to downgrading.'] },
    ],
    explanation: 'The dominant risk is silent, unbounded quality degradation with no observability or terminal state. Always escalating to costlier models is its own anti-pattern; removing fallback abandons resilience; a missing same-model retry is minor next to the lack of a floor and logging.',
  },
  {
    id: 'd1-009', domain: 'd1', topic: 'Parallelization workflows',
    archetype: 'best-practice', difficulty: 'scenario',
    dimensions: ['judgment'], claudeSpecific: false,
    stem: [
      'You must summarize 200 independent documents with Claude as fast as possible. Which approach is recommended?',
      'Two hundred unrelated documents each need a summary, and throughput matters. What is the recommended design?',
    ],
    correctIndex: 0,
    options: [
      { practice: ['Fan the work out into concurrent per-document calls, then aggregate the summaries.', 'Process the independent documents in parallel and combine the results.'],
        timed: ['Issue the document summaries concurrently and merge them afterward.'] },
      { practice: ['Concatenate all 200 documents into a single prompt and ask for every summary at once.'],
        timed: ['Put all 200 documents in one prompt and request all summaries together.', 'Concatenate every document into a single request for all summaries at once.'] },
      { practice: ['Summarize them one at a time within a single ongoing conversation.'],
        timed: ['Handle the documents sequentially inside one chat thread.', 'Summarize them one by one in a single conversation.'] },
      { practice: ['Have one agent loop over the documents, re-reading all prior ones each pass.'],
        timed: ['Use a single agent that re-ingests every earlier document on each iteration.', 'Loop one agent across the docs, re-reading all earlier ones each pass.'] },
    ],
    explanation: 'Independent work should be parallelized and aggregated — the textbook parallelization workflow. One mega-prompt blows the context window and mixes unrelated content; sequential processing and a re-reading loop are needlessly slow and accumulate irrelevant context.',
  },
  {
    id: 'd1-010', domain: 'd1', topic: 'Parallelization workflows',
    archetype: 'tradeoff', difficulty: 'scenario',
    dimensions: ['judgment'], claudeSpecific: false,
    stem: [
      'Parallelizing 50 subagent calls would be fastest, but you are near your API rate limit and the calls hit a shared rate-limited downstream service. What is the best call?',
      'Fifty independent calls could run at once, but rate limits (yours and a shared downstream) are a real constraint. Which approach balances speed and reliability?',
    ],
    correctIndex: 0,
    options: [
      { practice: ['Use bounded concurrency — a worker pool with a sensible cap — plus backoff on rate-limit responses.', 'Cap parallelism with a concurrency limit and back off when limits are hit.'],
        timed: ['Run a limited number in flight at once via a pool, with retry/backoff on throttling.'] },
      { practice: ['Fire all 50 at once to minimize wall-clock time and ignore the limits.'],
        timed: ['Launch every call simultaneously for maximum speed regardless of limits.', 'Fire all 50 at once and ignore the rate limits.'] },
      { practice: ['Run all 50 strictly sequentially to be completely safe.'],
        timed: ['Process the calls one by one to fully avoid throttling.', 'Execute all 50 strictly in sequence to stay safe.'] },
      { practice: ['Remove the dependency by never caching any downstream results.'],
        timed: ['Stop caching downstream results to sidestep the dependency.', 'Remove all downstream caching to break the dependency.'] },
    ],
    explanation: 'Bounded concurrency with backoff captures most of the parallel speedup while respecting rate limits — the right judgment under constraints. Unbounded fan-out trips the limits and triggers 429s; full serialization is needlessly slow; dropping caching makes downstream pressure worse, not better.',
  },
  {
    id: 'd1-011', domain: 'd1', topic: 'Chaining workflows',
    archetype: 'what-breaks', difficulty: 'scenario',
    dimensions: ['critical-eval'], claudeSpecific: false,
    stem: [
      'A chain passes each step’s raw, verbose output as the next step’s input. By step 5 the prompt is enormous and the model fixates on irrelevant earlier detail. What is the fix?',
      'In a multi-step chain, every step forwards its full raw output; later steps drown in earlier noise. Which change addresses the root cause?',
    ],
    correctIndex: 0,
    options: [
      { practice: ['Transform each step’s output into the distilled, structured result the next step actually needs before passing it on.', 'Extract a compact, structured hand-off between steps instead of forwarding raw output.'],
        timed: ['Reduce each step to the specific structured fields the following step requires.'] },
      { practice: ['Skip some middle steps to keep the final prompt shorter.'],
        timed: ['Drop intermediate steps so the last prompt stays small.', 'Skip some middle steps to shorten the final prompt.'] },
      { practice: ['Move to a larger-context model so the growing prompt still fits.'],
        timed: ['Adopt a bigger context window to accommodate the expanding prompt.', 'Upgrade to a model with more context so the longer prompt fits.'] },
      { practice: ['Raise the temperature so the model is less rigid about earlier detail.'],
        timed: ['Increase temperature to loosen the model’s focus on prior content.', 'Raise temperature so the model fixates less on earlier detail.'] },
    ],
    explanation: 'The flaw is forwarding raw verbose output; the fix is a clean, distilled hand-off between chain steps. Skipping steps changes behavior and loses work; a bigger context only delays the noise problem; temperature does not address irrelevant accumulated detail.',
  },
  {
    id: 'd1-012', domain: 'd1', topic: 'Routing workflows',
    archetype: 'best-practice', difficulty: 'scenario',
    dimensions: ['judgment'], claudeSpecific: false,
    stem: [
      'Incoming requests range from trivial FAQ lookups to complex multi-step analysis. Which design handles this mix well?',
      'A service receives both one-line FAQ questions and heavy analytical tasks. What is the recommended way to handle the range?',
    ],
    correctIndex: 0,
    options: [
      { practice: ['A lightweight router classifies each request and directs it to the appropriate handler and model tier.', 'Use a cheap classifier to route requests to the right-sized handler/model.'],
        timed: ['Add a fast routing step that sends each request to a matching handler and model capability.'] },
      { practice: ['Send every request to the most capable model to be safe.'],
        timed: ['Route all requests to the top model regardless of difficulty.', 'Default every request to the strongest model just to be safe.'] },
      { practice: ['Send every request to the cheapest model to minimize cost.'],
        timed: ['Handle everything with the smallest model to keep cost lowest.', 'Direct all requests to the cheapest model for minimum cost.'] },
      { practice: ['Handle all request types in one large prompt that tries to cover every case.'],
        timed: ['Use a single catch-all prompt that addresses every possible request type.', 'Cover all request types in one large general-purpose prompt.'] },
    ],
    explanation: 'Routing by classified complexity to the right tier optimizes both cost and quality — the standard routing workflow. Top-model-everywhere wastes money on trivial calls; cheapest-everywhere fails the hard ones; one mega-prompt is unfocused and brittle.',
  },
  {
    id: 'd1-013', domain: 'd1', topic: 'Routing workflows',
    archetype: 'tradeoff', difficulty: 'scenario',
    dimensions: ['judgment'], claudeSpecific: false,
    stem: [
      'Your router (a cheap classifier call) adds latency and occasionally misroutes hard questions to the cheap handler. Stakeholders want low cost and high accuracy on hard cases. Best adjustment?',
      'A cheap routing model sometimes sends difficult questions to the low-tier handler. You need cost control without sacrificing accuracy on the hard ones. What is the best change?',
    ],
    correctIndex: 0,
    options: [
      { practice: ['Escalate to the stronger handler whenever the classifier is low-confidence, accepting slightly higher cost on ambiguous cases.', 'Route by confidence: when the router is unsure, send the request to the higher tier.'],
        timed: ['Gate on classifier confidence — uncertain requests go to the more capable handler.'] },
      { practice: ['Remove the router and send everything to the cheap handler to cut cost.'],
        timed: ['Drop routing and handle all requests with the low tier for savings.', 'Remove the router and send everything to the cheap handler.'] },
      { practice: ['Remove the router and send everything to the expensive handler to be safe.'],
        timed: ['Eliminate routing and send all requests to the top tier.', 'Drop the router and route everything to the expensive handler.'] },
      { practice: ['Accept the misroutes since the router is cheaper than fixing them.'],
        timed: ['Tolerate the wrong routes because the classifier is inexpensive.', 'Accept the misroutes since the cheap router saves money.'] },
    ],
    explanation: 'Confidence-gated escalation keeps most traffic cheap while protecting accuracy on the cases that matter — the right tradeoff. Cheap-for-all sacrifices the hard cases; expensive-for-all defeats the routing’s purpose; accepting misroutes ignores the stated accuracy requirement.',
  },
  {
    id: 'd1-014', domain: 'd1', topic: 'Cost optimization',
    archetype: 'tradeoff', difficulty: 'scenario',
    dimensions: ['judgment'], claudeSpecific: false,
    stem: [
      'A pipeline runs every step on the top-tier model for quality. Cost is unsustainable. Most steps are simple extraction; one step is genuinely hard reasoning. Best optimization?',
      'An all-Opus pipeline is too expensive. Nearly all steps are easy extraction, with a single hard reasoning step. How should you cut cost?',
    ],
    correctIndex: 0,
    options: [
      { practice: ['Right-size per step: a smaller/faster model for the simple extraction steps, reserving the top tier for the hard reasoning step.', 'Tier the models by step difficulty — cheap models for extraction, top tier only where reasoning is hard.'],
        timed: ['Match each step to the cheapest model that handles it, keeping the strongest model for the one demanding step.'] },
      { practice: ['Move every step to the cheapest model to maximize savings.'],
        timed: ['Downgrade all steps to the smallest model for the biggest cut.', 'Shift every step to the smallest model for maximum savings.'] },
      { practice: ['Keep the top model everywhere but arbitrarily delete some steps to save calls.'],
        timed: ['Leave all steps on the top tier and just remove some steps to reduce volume.', 'Keep top-tier everywhere but delete some steps to cut calls.'] },
      { practice: ['Request a rate-limit increase so the expensive calls run faster.'],
        timed: ['Ask for higher rate limits to speed up the costly calls.', 'Request a rate-limit increase so the expensive calls go faster.'] },
    ],
    explanation: 'Per-step model tiering captures large savings while protecting the one step that needs reasoning power — the core cost-optimization judgment. Cheapest-everywhere fails the hard step; deleting steps changes behavior; a rate-limit bump affects throughput, not cost per call.',
  },
  {
    id: 'd1-015', domain: 'd1', topic: 'Cost optimization',
    archetype: 'critique-output', difficulty: 'hard',
    dimensions: ['critical-eval', 'judgment'], claudeSpecific: false,
    stem: [
      'To cut cost, an AI assistant suggests: route all requests through the cheapest model, disable retries, and remove output validation. What is the right critique?',
      'An AI proposes saving money by using only the cheapest model, turning off retries, and dropping validation. How should you respond in review?',
    ],
    correctIndex: 0,
    options: [
      { practice: ['It trades away reliability and quality for cost; cut spend instead via prompt caching, genuine batching, and per-step model tiering while keeping retries and validation.', 'Reject it: reduce cost through caching, batching, and right-sizing — not by removing the safeguards that keep output trustworthy.'],
        timed: ['The proposal sacrifices correctness for savings; achieve cost cuts with caching, batchable work, and tiered models while retaining retries and checks.'] },
      { practice: ['Agree fully — minimizing cost outweighs reliability concerns here.'],
        timed: ['Accept it as-is since cost reduction is the priority.', 'Approve the plan because lowering cost matters most.'] },
      { practice: ['It does not go far enough — also remove logging and monitoring to save more.'],
        timed: ['Extend it by cutting logging and monitoring for additional savings.', 'Go further and remove monitoring and logging to save more.'] },
      { practice: ['The only real issue is the model choice; disabling retries and validation is fine.'],
        timed: ['Just revisit the model tier; dropping retries and validation is acceptable.', 'Only the model choice needs review; removing retries and validation is fine.'] },
    ],
    explanation: 'The proposal removes the safeguards (retries, validation) that protect correctness and downgrades quality blindly. Sound cost control comes from caching, real batching, and tiering. Agreeing, cutting monitoring too, or excusing the removed safeguards all accept unacceptable reliability loss.',
  },
  {
    id: 'd1-016', domain: 'd1', topic: 'Tool orchestration',
    archetype: 'foundational', difficulty: 'core',
    dimensions: [], claudeSpecific: false,
    stem: [
      'In a tool-using agent, what is the correct control flow for a single tool call?',
      'Which sequence correctly describes one tool-use round in an agent?',
    ],
    correctIndex: 0,
    options: [
      { practice: ['The model requests a tool with input, the application executes the tool and returns the result to the model, and the model continues.', 'Model emits a tool call → app runs the tool → app feeds the result back → model proceeds.'],
        timed: ['The model asks to call a tool, the host runs it and hands back the output, then the model resumes generation.'] },
      { practice: ['The model executes the tool itself and returns the final answer directly.'],
        timed: ['The model runs the tool internally and produces the result on its own.', 'The model executes the tool itself and returns the final answer.'] },
      { practice: ['The application guesses the answer without ever invoking the requested tool.'],
        timed: ['The host fabricates the result rather than calling the tool.', 'The application guesses the answer without invoking the tool.'] },
      { practice: ['The tool invokes the model, which then decides whether to run.'],
        timed: ['The tool calls the model first, which then chooses to execute.', 'The tool triggers the model, which then decides whether to run.'] },
    ],
    explanation: 'Tool use is a round trip: the model requests, the application executes and returns the tool result, and the model continues. The model never executes tools itself, the app must not fabricate results, and the direction is model → app → model, not the reverse.',
  },

  // ─── Domain 2 — Claude Code Configuration & Workflows (12) ──────────────────
  {
    id: 'd2-001', domain: 'd2', topic: 'CLAUDE.md hierarchies',
    archetype: 'what-breaks', difficulty: 'scenario',
    dimensions: ['critical-eval', 'judgment'], claudeSpecific: true,
    stem: [
      'A team pastes its entire API reference, style guide, and project history into one 5,000-line project CLAUDE.md. Claude now ignores instructions and the context window fills fast. What is the fix?',
      'A project’s CLAUDE.md has grown to thousands of lines of reference material, and the model increasingly misses its instructions. Which change addresses the root cause?',
    ],
    correctIndex: 0,
    options: [
      { practice: ['Keep CLAUDE.md concise and high-signal; move reference material into docs Claude reads on demand, and use the global/project/local layers for what belongs at each.', 'Trim it to durable, high-value guidance and link out to detailed docs rather than inlining everything.'],
        timed: ['Pare CLAUDE.md down to essential, lasting instructions; let Claude pull detailed references on demand and place each rule at the right global/project/local layer.'] },
      { practice: ['Move the same thousands of lines into the global ~/.claude CLAUDE.md so every project inherits them.'],
        timed: ['Relocate the entire file to the global CLAUDE.md so all projects load it.', 'Push the whole thing into the user-level CLAUDE.md for every repo to inherit.'] },
      { practice: ['Keep the giant file but add a line telling Claude to read it carefully.'],
        timed: ['Leave it huge and instruct Claude to read all of it attentively.', 'Retain the full file and just ask the model to pay closer attention.'] },
      { practice: ['Paste the full reference into every prompt instead of the file.'],
        timed: ['Inline the entire reference into each prompt rather than the file.', 'Attach the whole reference text to every message you send.'] },
    ],
    explanation: 'CLAUDE.md is always-on context; bloating it crowds the window and dilutes instructions. Keep it lean and layered, linking to docs read on demand. Moving the bloat to the global file makes every project pay the cost; telling Claude to "read carefully" or pasting it into prompts is the same bloat relocated.',
  },
  {
    id: 'd2-002', domain: 'd2', topic: 'Custom slash commands',
    archetype: 'best-practice', difficulty: 'core',
    dimensions: [], claudeSpecific: true,
    stem: [
      'What is the correct way to create a reusable custom slash command in Claude Code?',
      'How do you define a shareable custom slash command in Claude Code?',
    ],
    correctIndex: 0,
    options: [
      { practice: ['Add a Markdown file under .claude/commands/ (project) or ~/.claude/commands/ (personal); its body is the prompt, with $ARGUMENTS for inputs.', 'Create a command file in .claude/commands/<name>.md whose contents are the prompt template.'],
        timed: ['Place a Markdown file in .claude/commands/ whose body becomes the command prompt, using $ARGUMENTS for parameters.'] },
      { practice: ['Add a shell-script entry to settings.json and call it as a command.'],
        timed: ['Register a shell script in settings.json to act as the command.', 'Wire the command up as a script reference inside settings.json.'] },
      { practice: ['Write the instructions as a section in CLAUDE.md and trigger them by name.'],
        timed: ['Define it as a labeled block in CLAUDE.md and invoke by that label.', 'Put the command text in CLAUDE.md and call it by heading.'] },
      { practice: ['Install an MCP server that exposes the command as a tool.'],
        timed: ['Stand up an MCP server so the command appears as a tool.', 'Expose the command through a custom MCP server tool.'] },
    ],
    explanation: 'Custom slash commands are Markdown files in .claude/commands/ (project) or ~/.claude/commands/ (personal); the file body is the prompt and $ARGUMENTS injects inputs. settings.json is for permissions/hooks, CLAUDE.md is always-on guidance rather than an invokable command, and MCP servers expose tools, not slash commands.',
  },
  {
    id: 'd2-003', domain: 'd2', topic: 'Agent Skills',
    archetype: 'best-practice', difficulty: 'scenario',
    dimensions: ['judgment'], claudeSpecific: true,
    stem: [
      'You want a reusable workflow that Claude automatically invokes when it is relevant, and that the whole team shares. How should you build it?',
      'A recurring workflow should trigger on its own when applicable and be shared across the team. What is the recommended mechanism?',
    ],
    correctIndex: 0,
    options: [
      { practice: ['Build an Agent Skill: a SKILL.md with name/description frontmatter (the description drives automatic invocation), committed to the repo so the team shares it.', 'Author a skill file whose description tells Claude when to use it, and commit it for the team.'],
        timed: ['Create an Agent Skill — a SKILL.md whose frontmatter description triggers auto-invocation — and commit it to the repo for sharing.'] },
      { practice: ['Make a custom slash command, since it does the same thing automatically.'],
        timed: ['Use a slash command, which Claude will run on its own when relevant.', 'A custom slash command will auto-trigger the workflow when needed.'] },
      { practice: ['Add a long section to CLAUDE.md describing the workflow steps.'],
        timed: ['Document the workflow as a big CLAUDE.md section.', 'Write the steps into CLAUDE.md so they are always loaded.'] },
      { practice: ['Configure a PostToolUse hook to run the workflow.'],
        timed: ['Set up a hook to execute the workflow after tool calls.', 'Trigger the workflow from a PostToolUse hook.'] },
    ],
    explanation: 'Agent Skills are reusable, description-triggered units (SKILL.md) that Claude auto-invokes when the description matches, and you commit them to share. Slash commands must be invoked manually; a CLAUDE.md section is always-on prose, not a packaged skill; hooks fire on tool events, not on task relevance.',
  },
  {
    id: 'd2-004', domain: 'd2', topic: 'Hooks',
    archetype: 'what-breaks', difficulty: 'scenario',
    dimensions: ['critical-eval', 'judgment'], claudeSpecific: true,
    stem: [
      "To 'catch regressions early,' an engineer adds a PreToolUse hook that runs the full test suite before every file edit. Sessions become unusably slow. What is the better design?",
      "A team's PreToolUse hook executes the entire test suite ahead of each edit, grinding sessions to a halt. How should this be restructured?",
    ],
    correctIndex: 0,
    options: [
      { practice: ['Move full-suite testing to a PostToolUse or Stop hook (or CI), and keep PreToolUse checks fast and targeted.', 'Run the suite after edits / at session stop or in CI; reserve PreToolUse for cheap, scoped validation.'],
        timed: ['Relocate the heavy test run to a post-edit or stop-time hook (or the CI pipeline) and limit pre-edit hooks to quick, narrow checks.'] },
      { practice: ['Keep the hook but disable tests that are slow.'],
        timed: ['Leave the PreToolUse hook and just turn off the slower tests.', 'Keep the pre-edit hook but switch off the slow tests.'] },
      { practice: ['Replace the hook with a CLAUDE.md instruction telling the model to run tests.'],
        timed: ['Drop the hook and add a CLAUDE.md note asking the model to test before editing.', 'Swap the hook for a CLAUDE.md instruction to run tests first.'] },
      { practice: ['Increase the hook timeout so the suite has time to finish.'],
        timed: ['Raise the hook timeout so the full suite can complete each time.', 'Extend the hook timeout to let the whole suite finish every edit.'] },
    ],
    explanation: 'Hooks that block every tool call must be fast and targeted; full suites belong in PostToolUse/Stop hooks or CI. Disabling tests trades safety for speed; a CLAUDE.md instruction is advisory not enforced; a longer timeout keeps the latency. Recognizing that the hook trigger point is the flaw is the judgment signal.',
  },
  {
    id: 'd2-005', domain: 'd2', topic: 'MCP server integration',
    archetype: 'critique-output', difficulty: 'scenario',
    dimensions: ['critical-eval'], claudeSpecific: true,
    stem: [
      'An engineer wires up an MCP server by hardcoding its API token directly in the committed .mcp.json and grants the server access to every tool. What is the right critique?',
      'A committed .mcp.json contains a plaintext API token and gives the MCP server full tool access. How should you respond in review?',
    ],
    correctIndex: 0,
    options: [
      { practice: ['Reject it: the API token must not be committed — reference it from an environment variable — and the server should be granted only the tools it needs (least privilege).', 'Do not commit the secret; use env-var interpolation in .mcp.json and scope the server to least-privilege tool access.'],
        timed: ['Push back — keep the token out of version control via an env-var reference and limit the server to only the tools it requires.'] },
      { practice: ['It is fine because .mcp.json stays on the developer machine and is never shared.'],
        timed: ['No issue — .mcp.json is local and never leaves the machine.', 'Acceptable, since the config file is private to the developer.'] },
      { practice: ['The only problem is that the server should expose more tools to be useful.'],
        timed: ['The sole concern is that it needs additional tools.', 'It just needs broader tool access to be worthwhile.'] },
      { practice: ['Move the hardcoded token into CLAUDE.md so it is at least documented.'],
        timed: ['Relocate the token into CLAUDE.md for documentation.', 'Record the token in CLAUDE.md instead.'] },
    ],
    explanation: 'Two real problems: a committed secret (project .mcp.json is shared and version-controlled, so use env-var references) and over-broad tool grants (apply least privilege). Calling it local is wrong, "needs more tools" inverts the issue, and moving the token to CLAUDE.md still commits the secret.',
  },
  {
    id: 'd2-006', domain: 'd2', topic: 'GitHub integration',
    archetype: 'best-practice', difficulty: 'scenario',
    dimensions: ['judgment'], claudeSpecific: true,
    stem: [
      'You want Claude to automatically review pull requests and flag regressions before merge. What is the recommended setup?',
      'A team wants automated Claude PR reviews that comment on problems prior to merge. Which approach is recommended?',
    ],
    correctIndex: 0,
    options: [
      { practice: ['Use the Claude Code GitHub app/Action triggered on pull_request, with scoped repository permissions.', 'Wire up the GitHub Action to run on PRs and post review comments, using least-privilege tokens.'],
        timed: ['Configure the Claude Code GitHub Action to run on pull_request events and comment, with narrowly scoped permissions.'] },
      { practice: ['Paste each PR diff into an interactive chat by hand for review.'],
        timed: ['Manually copy every diff into a chat session to review.', 'Hand-paste each pull request diff into the chat each time.'] },
      { practice: ['Give the automation your personal GitHub account password.'],
        timed: ['Hand it your personal GitHub password for access.', 'Authenticate it with your own account password.'] },
      { practice: ['Run reviews only on direct pushes to main, after the merge has landed.'],
        timed: ['Trigger reviews solely on post-merge pushes to main.', 'Only review on pushes to main once changes are merged.'] },
    ],
    explanation: 'The GitHub Action on pull_request with scoped permissions is the supported, automated path and catches issues before merge. Manual diff-pasting does not scale; sharing a personal password is a security anti-pattern (use scoped tokens/the app); reviewing only post-merge on main defeats the purpose of PR review.',
  },
  {
    id: 'd2-007', domain: 'd2', topic: 'Context management',
    archetype: 'what-breaks', difficulty: 'scenario',
    dimensions: ['critical-eval', 'judgment'], claudeSpecific: true,
    stem: [
      'A long Claude Code session has drifted — earlier, unrelated tasks keep confusing the model on the current one. What is the right action?',
      'Mid-session, residue from earlier unrelated tasks is biasing Claude on a new task. What should you do?',
    ],
    correctIndex: 0,
    options: [
      { practice: ['Run /clear to reset context before the new task (or /compact to summarize) so prior unrelated work stops interfering.', 'Clear or compact the context so the stale earlier tasks no longer bias the current one.'],
        timed: ['Reset the context with /clear (or summarize with /compact) before starting the new task so old work stops interfering.'] },
      { practice: ['Keep the session going and prompt more forcefully about the current task.'],
        timed: ['Stay in the session and just insist harder on the current task.', 'Continue and re-emphasize the current task more strongly.'] },
      { practice: ['Open many more files so the model has additional context to sort it out.'],
        timed: ['Load more files to give the model extra context.', 'Add more open files so it can figure things out.'] },
      { practice: ['Switch to a larger-context model and keep all the accumulated history.'],
        timed: ['Move to a bigger context window and retain everything.', 'Adopt a larger-context model and keep the full history.'] },
    ],
    explanation: 'Context discipline: /clear (or /compact) removes stale, unrelated context that is biasing the current task. Prompting harder fights the noise instead of removing it; adding files or a bigger context window increases the clutter rather than clearing it.',
  },
  {
    id: 'd2-008', domain: 'd2', topic: 'Claude Code SDK',
    archetype: 'foundational', difficulty: 'core',
    dimensions: [], claudeSpecific: true,
    stem: [
      'What does the Claude Code SDK enable?',
      'What is the Claude Code SDK for?',
    ],
    correctIndex: 0,
    options: [
      { practice: ['It lets you run Claude Code programmatically / headless — e.g. invoked from scripts or CI — outside the interactive terminal.', 'It exposes Claude Code for non-interactive, scripted use such as automation pipelines.'],
        timed: ['It enables programmatic, headless use of Claude Code from scripts or CI rather than the interactive CLI.'] },
      { practice: ['It is a visual theme/skin for the Claude Code terminal UI.'],
        timed: ['It is just a UI theme for the CLI.', 'It changes the terminal appearance of Claude Code.'] },
      { practice: ['It replaces the Anthropic API for building general chat applications.'],
        timed: ['It is a drop-in replacement for the Anthropic chat API.', 'It supersedes the API for building chat apps.'] },
      { practice: ['It is required before you can use any slash commands.'],
        timed: ['Slash commands only work once the SDK is installed.', 'You must install the SDK to run slash commands.'] },
    ],
    explanation: 'The Claude Code SDK is for programmatic/headless invocation (scripts, CI), not a theme, not an API replacement for chat apps, and not a prerequisite for slash commands (which work in the normal CLI).',
  },
  {
    id: 'd2-009', domain: 'd2', topic: 'Subagents',
    archetype: 'what-breaks', difficulty: 'scenario',
    dimensions: ['critical-eval', 'judgment'], claudeSpecific: true,
    stem: [
      'A single Claude session runs a huge multi-part research task; its context fills with file dumps and search output, degrading reasoning on the later parts. What is the better approach?',
      'One session is doing a large multi-step investigation and its context is saturated with raw output, hurting later steps. Which design helps most?',
    ],
    correctIndex: 0,
    options: [
      { practice: ['Delegate independent sub-tasks to subagents that return only their conclusions, keeping the main session’s context lean.', 'Use subagents for the heavy sub-tasks so the main context receives summaries, not raw dumps.'],
        timed: ['Hand the bulky sub-tasks to subagents that report back only conclusions, so the main context stays focused.'] },
      { practice: ['Switch to the largest-context model and keep everything in the one session.'],
        timed: ['Move to a bigger context window and do it all in one session.', 'Adopt the largest-context model and keep a single session.'] },
      { practice: ['Turn off extended thinking to leave more room for file contents.'],
        timed: ['Disable thinking so more files fit in context.', 'Stop using extended thinking to free up context for files.'] },
      { practice: ['Put the entire task and all files into one giant prompt up front.'],
        timed: ['Load the whole task and every file in a single prompt.', 'Cram the full task and all files into one prompt.'] },
    ],
    explanation: 'Subagents isolate heavy sub-tasks and return distilled conclusions, protecting the main context — the standard context-management pattern. A bigger context only delays saturation; disabling thinking trades reasoning for room; one giant prompt is the very bloat that caused the degradation.',
  },
  {
    id: 'd2-010', domain: 'd2', topic: 'Tool access restrictions',
    archetype: 'critique-output', difficulty: 'scenario',
    dimensions: ['critical-eval'], claudeSpecific: true,
    stem: [
      'A skill that only needs to read files is configured with full tool access, including Bash and Write. What is the right critique?',
      'Reviewing a read-only skill that has nonetheless been granted Bash and Write — what is the main problem?',
    ],
    correctIndex: 0,
    options: [
      { practice: ['It violates least privilege — restrict the skill’s allowed tools to what it needs (read-only here) to limit blast radius.', 'Scope the skill to only the tools required; a read-only skill should not have Bash or Write.'],
        timed: ['Apply least privilege: limit the skill’s tool access to read-only, removing the Bash and Write it does not need.'] },
      { practice: ['It is fine — more available tools simply make the skill more capable.'],
        timed: ['No problem — extra tools just add capability.', 'Acceptable, since broader access is more powerful.'] },
      { practice: ['The real gap is that it also needs network access to be complete.'],
        timed: ['It actually needs network access added too.', 'The missing piece is network access.'] },
      { practice: ['Tool restrictions do not apply to skills, so it does not matter.'],
        timed: ['Skills ignore tool restrictions anyway.', 'Tool scoping has no effect on skills.'] },
    ],
    explanation: 'A read-only skill granted Bash and Write violates least privilege and widens blast radius; scope its allowed tools to what it needs. More tools is not "more capable" when unneeded, adding network access expands the problem, and tool restrictions absolutely do apply to skills.',
  },
  {
    id: 'd2-011', domain: 'd2', topic: 'Configuration files',
    archetype: 'best-practice', difficulty: 'core',
    dimensions: [], claudeSpecific: true,
    stem: [
      'Where do project-shared permissions and hooks belong versus personal, machine-specific overrides?',
      'How should team-wide Claude Code config be separated from an individual’s local overrides?',
    ],
    correctIndex: 0,
    options: [
      { practice: ['Shared, committed settings go in .claude/settings.json; personal machine-specific overrides go in settings.local.json (gitignored).', 'Team-wide permissions/hooks belong in settings.json; individual overrides in settings.local.json.'],
        timed: ['Put team-shared config in .claude/settings.json and personal overrides in the gitignored settings.local.json.'] },
      { practice: ['Put all configuration, shared and personal, into CLAUDE.md.'],
        timed: ['Keep every setting in CLAUDE.md.', 'Store all config, shared or not, in CLAUDE.md.'] },
      { practice: ['Keep everything in settings.local.json and commit it for the team.'],
        timed: ['Put all settings in settings.local.json and check it in.', 'Commit settings.local.json so the team shares it.'] },
      { practice: ['Hardcode the permissions into each slash-command file.'],
        timed: ['Embed permissions in every command file.', 'Repeat the permission config inside each command.'] },
    ],
    explanation: 'settings.json is the committed, shared project config; settings.local.json holds personal, gitignored overrides. CLAUDE.md is guidance not config; committing settings.local.json defeats its purpose; duplicating permissions into each command is unmaintainable.',
  },
  {
    id: 'd2-012', domain: 'd2', topic: 'Context management',
    archetype: 'critique-output', difficulty: 'hard',
    dimensions: ['critical-eval', 'judgment'], claudeSpecific: true,
    stem: [
      "To 'never lose context,' an engineer disables /compact and auto-compaction and keeps a single session open for days across many unrelated tasks. What is the right critique?",
      'Someone argues that disabling compaction and keeping one perpetual session maximizes context retention. How should you respond?',
    ],
    correctIndex: 0,
    options: [
      { practice: ['Long undifferentiated context degrades focus and inflates cost; use /clear between unrelated tasks and let compaction summarize — disciplined context beats hoarding it.', 'Keeping one ballooning session hurts quality and cost; clear between tasks and allow /compact rather than disabling it.'],
        timed: ['Hoarding context erodes focus and raises cost; re-enable compaction and /clear between unrelated tasks instead of running one endless session.'] },
      { practice: ['It is the right approach — more retained context is always better.'],
        timed: ['Correct call, since more context is strictly better.', 'Sound approach — retaining everything always helps.'] },
      { practice: ['The only fix needed is to switch to a larger model.'],
        timed: ['Just move to a bigger model and it is fine.', 'A larger model is the sole change required.'] },
      { practice: ['They should instead restart the session after every single message.'],
        timed: ['Better to start a fresh session on each message.', 'Reset the session for every individual message.'] },
    ],
    explanation: 'Disabling compaction and never clearing produces a bloated context that degrades focus and cost; the discipline is /clear between unrelated tasks plus compaction summaries. More context is not always better, a bigger model does not fix clutter, and resetting every message destroys useful continuity.',
  },
]
